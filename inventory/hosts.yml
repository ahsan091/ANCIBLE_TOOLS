---
# Inventory file for GuardianEye SOC Stack deployment
# By default, targets localhost for single-node deployment

all:
  children:
    soc:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
